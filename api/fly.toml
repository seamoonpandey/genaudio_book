app = "genaudi-api"
primary_region = "sin"

[build]
  dockerfile = "Dockerfile"

[env]
  DATA_DIR = "/data"
  FRONTEND_ORIGIN = "https://genaudi.pages.dev"

[[mounts]]
  source = "data"
  destination = "/data"

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = "stop"
  auto_start_machines = true
  min_machines_running = 1  # worker claims jobs over private network; internal traffic can't auto-start

[[vm]]
  size = "shared-cpu-1x"
  memory = "512mb"
